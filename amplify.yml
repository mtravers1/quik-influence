version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm i -g npm@latest
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
